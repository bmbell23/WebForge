# Android / Gradle
android/.gradle/
android/build/
android/app/build/
android/local.properties

# Electron / node
windows/node_modules/
windows/dist/

# Build artifacts are staged here for the release server — not tracked
releases/*
!releases/.gitkeep

# Secrets
.env

# webforge_sync container store (root-owned, runtime data)
/data/
